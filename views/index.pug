doctype html
html
	head
		title="Sample App"
		link(rel='stylesheet', href='assets/style.css', type='text/css')
	body
		main
			div
				h1 Hello, Friend!
				p.message
					| This is a sample app that does nothing.
					br
					| To log in simply state your name and click the button.
				form.main-form(action='/auth/google', method='GET')
					input.name(type='text', name='name')
					input.button(type='submit', value='Login')
